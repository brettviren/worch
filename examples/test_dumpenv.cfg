[start]
groups = gnuprograms
group = gnuprograms
install_dir = {PREFIX}/{package}/{version}/{tagsdashed}

[group gnuprograms]
features = dumpenv
packages = hello

[package hello]
version = 2.8

[keytype]
groups = group
packages = package

