# build the sub-suite needed for g4lbne

[start]
groups = buildtools, g4lbne
includes = defaults.cfg, buildtools.cfg, externals.cfg

[group g4lbne]
environment = group:buildtools, group:compiler
#packages = xrootd, python, root, geant, g4lbne
packages = xrootd, python, root
