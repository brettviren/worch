[start]
groups = foo

[group foo]
packages = bar

[package bar]
version = 0
features = dumpenv
dumpenv_cmd = /usr/bin/env
buildenv_FOO = buildfoo
buildenv_BAR = buildbar
export_FOO = exportfoo
export_BAZ = exportbaz

[keytype]
groups = group
packages = package
