[start]
groups = thegroup
features = dumpenv
build_dir = build

[group thegroup]
packages = thepackage
dumpenv_need = Foo
valueb = bee

[package thepackage]
version = 1.0
valuea = aye

[keytype]
groups = group
packages = package
